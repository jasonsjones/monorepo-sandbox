{
    "private": true,
    "workspaces": {
        "packages": [
            "packages/*"
        ]
    },
    "scripts": {
        "dev": "lerna run dev --parallel --scope @orion/api --scope @orion/app",
        "lint": "lerna run lint",
        "test": "lerna run test"
    },
    "devDependencies": {
        "lerna": "^3.10.7"
    },
    "name": "monorepo-sandbox"
}
