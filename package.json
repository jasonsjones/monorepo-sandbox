{
    "private": true,
    "name": "monorepo-sandbox",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "bootstrap": "lerna bootstrap",
        "dev": "lerna run dev --stream",
        "dev:api": "lerna run dev --stream --scope @orion/api",
        "dev:app": "lerna run dev --stream --scope @orion/app",
        "build": "lerna run build --stream",
        "lint": "lerna run lint --stream",
        "postinstall": "yarn bootstrap",
        "test": "lerna run test --stream",
        "test:api": "lerna run test --stream --scope @orion/api",
        "test:app": "lerna run test --stream --scope @orion/app",
        "test:e2e": "lerna run test:e2e --stream",
        "test:cypress": "lerna run cypress --stream --scope @orion/app"
    },
    "husky": {
        "hooks": {
            "pre-commit": "lint-staged"
        }
    },
    "lint-staged": {
        "*.{js,ts}": [
            "yarn lint",
            "git add"
        ]
    },
    "devDependencies": {
        "husky": "^3.0.0",
        "lerna": "^3.13.2",
        "lint-staged": "^9.0.2"
    }
}
